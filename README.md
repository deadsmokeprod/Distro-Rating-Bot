# Distro Rating Bot

Telegram-бот на **Python** и **aiogram v3** для учёта продаж и рейтингов. Две роли: **менеджер** и **продавец**. Менеджер регистрирует организации, обновляет базу из 1С и выгружает рейтинги. Продавцы регистрируются по ИНН и паролю организации, фиксируют продажи и видят рейтинги.

---

## Содержание

- [Возможности](#возможности)
- [Первый запуск](#первый-запуск)
- [Последующие запуски](#последующие-запуски)
- [Настройка бота](#настройка-бота)
- [Как пользоваться ботом](#как-пользоваться-ботом)
- [Интеграция с 1С](#интеграция-с-1с)
- [Модуль для 1С (расширение)](#модуль-для-1с-расширение)
- [Хранение данных](#хранение-данных)
- [Расписание задач](#расписание-задач)
- [Тестовые данные](#тестовые-данные)
- [Устранение неполадок](#устранение-неполадок)

---

## Возможности

### Менеджер

- **Регистрация организаций** — ИНН, наименование, автоматическая генерация пароля для продавцов.
- **Карточка организации** — просмотр, сброс пароля, список сотрудников (продавцов).
- **Обновление базы из 1С** — загрузка оборотов за текущий месяц или за произвольный период (до 60 дней). В запрос к 1С передаётся тип операции (например, «Передача между УОТ»).
- **Автообновление базы** — каждое воскресенье в 04:00 МСК (если задан `ONEC_URL`).
- **Выгрузка рейтингов в Excel** — за выбранный период (по месяцам) плюс лист «Текущий месяц».
- **Рассылка** — всем продавцам или только продавцам своих организаций.

### Продавец

- **Регистрация** — по ИНН организации, паролю и ФИО (организация должна быть создана менеджером).
- **Фиксация продаж** — выбор из списка незакреплённых продаж своей организации и закрепление за собой.
- **Профиль** — рейтинг за всё время, за текущий месяц, сравнение с прошлым месяцем, челлендж, лига.
- **Мировой рейтинг месяца** — таблица всех продавцов за текущий месяц.
- **Рейтинг компании за месяц** — рейтинг внутри своей организации за текущий месяц.
- **Челленджи** — двухнедельные цели (1–14 и 15–конец месяца), прогресс отображается в меню и профиле.
- **Лиги** — по процентилям мирового рейтинга текущего месяца (в стиле Duolingo).

### Общее

- **Техподдержка** — кнопка «Написать в техподдержку» (по `SUPPORT_USERNAME` или уведомление поддержке по `SUPPORT_USER_ID`).
- **Напоминания** — бот по расписанию (каждый день в 10:00 МСК, кроме тихих часов) может напоминать продавцам: не заходил 3+ дня, потерял место в рейтинге, близкий конкурент в компании.

---

## Первый запуск

### 1. Требования

- **Python 3.10+**
- Доступ в интернет (Telegram API, при необходимости — 1С по HTTP)

### 2. Клонирование и каталог

```bash
git clone https://github.com/deadsmokeprod/Distro-Rating-Bot.git
cd Distro-Rating-Bot
```

(Либо распакуйте архив с проектом и откройте этот каталог в терминале.)

### 3. Виртуальное окружение (рекомендуется)

**Windows (PowerShell):**
```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
```

**Linux / macOS:**
```bash
python3 -m venv .venv
source .venv/bin/activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 5. Настройка окружения

- Скопируйте файл с примером переменных окружения:
  ```bash
  copy .env.example .env
  ```
  (В Linux/macOS: `cp .env.example .env`.)

- Откройте `.env` в редакторе и заполните **обязательные** переменные:
  - `BOT_TOKEN` — токен бота от [@BotFather](https://t.me/BotFather).
  - `MANAGER_IDS` — ваш Telegram ID (или несколько через запятую). Как узнать ID: напишите боту [@userinfobot](https://t.me/userinfobot).
  - `SUPPORT_USER_ID` — Telegram ID пользователя техподдержки.

- Остальные переменные можно оставить по умолчанию или задать по необходимости (см. [Настройка бота](#настройка-бота)).

### 6. Запуск бота

```bash
python bot.py
```

В консоли должны появиться строки вида:
- `Scheduler started`
- `Start polling`
- `Run polling for bot @ВашБот ...`

После этого бот готов к работе. Остановка: **Ctrl+C** в терминале.

### 7. Проверка в Telegram

- Откройте бота в Telegram и нажмите **Start** (или отправьте `/start`).
- Если ваш Telegram ID указан в `MANAGER_IDS`, бот ответит: «Вы вошли как Менеджер» и покажет меню менеджера.
- Если ID не в списке менеджеров — бот предложит зарегистрироваться как продавец (для этого сначала нужно создать организацию от имени менеджера).

---

## Последующие запуски

1. Откройте каталог проекта в терминале:
   ```bash
   cd Distro-Rating-Bot
   ```

2. При использовании виртуального окружения — активируйте его:
   - Windows: `.\.venv\Scripts\Activate.ps1`
   - Linux/macOS: `source .venv/bin/activate`

3. Запустите бота:
   ```bash
   python bot.py
   ```

Остановка — **Ctrl+C**. Файл `.env` и база SQLite (`DB_PATH`) сохраняются между запусками, менять их не нужно.

**Запуск в фоне (пример для Windows PowerShell):**
```powershell
Start-Process python -ArgumentList "bot.py" -WorkingDirectory "D:\BOT2\Distro-Rating-Bot" -NoNewWindow
```

На Linux/macOS часто используют `systemd`, `screen` или `tmux` — настройка зависит от вашего сервера.

---

## Настройка бота

Все настройки задаются в файле **`.env`** в корне проекта. После изменения `.env` бота нужно перезапустить.

### Обязательные переменные

| Переменная        | Описание |
|-------------------|----------|
| `BOT_TOKEN`       | Токен бота от [@BotFather](https://t.me/BotFather). |
| `MANAGER_IDS`     | Telegram ID менеджеров через запятую, без пробелов (или с пробелами — они игнорируются). Пример: `123456789,987654321`. |
| `SUPPORT_USER_ID` | Telegram ID пользователя техподдержки (число). |

### Опциональные (Telegram и поддержка)

| Переменная        | Описание | По умолчанию |
|-------------------|----------|--------------|
| `SUPPORT_USERNAME` | Username техподдержки **без @** (например, `support_bot`). Если задан, под сообщениями будет кнопка «Написать в техподдержку» с ссылкой `https://t.me/support_bot`. Если не задан — по нажатию бот отправит уведомление в поддержку по `SUPPORT_USER_ID`. | — |

### Интеграция с 1С

| Переменная             | Описание | По умолчанию |
|------------------------|----------|--------------|
| `ONEC_URL`             | URL HTTP-сервиса 1С для загрузки оборотов. Пример: `http://192.168.0.217/1cerp_copy/hs/BotAPI/chz_turnover`. | — |
| `ONEC_OPERATION_TYPE`  | Тип операции, передаваемый в 1С (например, «Передача между УОТ»). | `Передача между УОТ` |
| `ONEC_USERNAME`        | Логин для Basic Auth к 1С (если 1С опубликована с проверкой доступа). | — |
| `ONEC_PASSWORD`        | Пароль для Basic Auth. В `.env` при спецсимволах лучше заключить в двойные кавычки, например: `"mWS4nD@x3zwe~\|}"`. | — |
| `ONEC_TIMEOUT_SECONDS` | Таймаут запроса к 1С в секундах. | `60` |

Если `ONEC_URL` не задан, пункт «Обновить базу» и автообновление по воскресеньям не будут обращаться к 1С (бот выдаст сообщение об ошибке при ручном обновлении).

### Геймификация и напоминания

| Переменная             | Описание | По умолчанию |
|------------------------|----------|--------------|
| `CHALLENGE_GROWTH_PCT` | Процент роста цели челленджа относительно прошлого месяца. | `20` |
| `CHALLENGE_BASE_VOLUME` | Минимальная цель челленджа в литрах, если в прошлом месяце продаж не было. | `10` |
| `QUIET_HOURS_START`    | Начало «тихих часов» (МСК), когда не отправляются напоминания. Формат: `ЧЧ:ММ`. | `19:00` |
| `QUIET_HOURS_END`      | Конец «тихих часов» (МСК). | `08:00` |

### Пути к файлам

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DB_PATH` | Путь к файлу SQLite (база данных). Каталог создаётся автоматически. | `./data/bot.sqlite3` |
| `LOG_PATH` | Путь к файлу логов. Каталог создаётся автоматически. | `./logs/bot.log` |

Файл **`.env`** загружается из каталога проекта (где лежит `bot.py`), а не из текущей рабочей папки терминала — бота можно запускать из любой директории, указав правильный рабочий каталог для `python bot.py`.

---

## Как пользоваться ботом

### Менеджер

1. **Вход**  
   Отправьте боту `/start`. Если ваш Telegram ID есть в `MANAGER_IDS`, появится сообщение «Вы вошли как Менеджер» и меню с кнопками.

2. **Зарегистрировать организацию**  
   - Нажмите «➕ Зарегистрировать организацию».  
   - Введите **ИНН** (10 или 12 цифр).  
   - Введите **наименование** организации (2–200 символов).  
   - Подтвердите создание. Бот покажет сгенерированный **пароль** — его нужно передать продавцам для входа.  
   - Пароль можно посмотреть или сбросить в карточке организации («📋 Мои организации» → выбрать организацию → «🔄 Сбросить пароль организации»).

3. **Обновить базу**  
   - «🔄 Обновить базу».  
   - Выберите **«📅 Текущий месяц»** или **«🗓️ Период ДДММГГГГ по ДДММГГГГ»**.  
   - Для произвольного периода введите, например: `01012026 по 31012026` (начало и конец периода без пробелов, до 60 дней).  
   - Бот отправит запрос в 1С и ответит: успех (количество полученных и записанных строк) или текст ошибки 1С/сети.

4. **Выгрузить рейтинги в Excel**  
   - «📤 Выгрузить рейтинги в EXCEL».  
   - Введите период в формате: **с ММ ГГГГ по ММ ГГГГ**, например: `с 01 2026 по 03 2026`.  
   - Бот пришлёт файл `.xlsx` с листами «Снапшоты» (месячные рейтинги) и «Текущий месяц».

5. **Рассылка**  
   - «📣 Рассылка продавцам» → выбрать «Всем продавцам» или «Продавцам моих компаний» → ввести текст → подтвердить отправку.

### Продавец

1. **Регистрация**  
   - Отправьте `/start`. Если вы не менеджер и ещё не зарегистрированы, бот спросит: «Ваша компания зарегистрирована?» — нажмите «✅ Да».  
   - Введите **ИНН** организации (как уточнил менеджер).  
   - Введите **пароль** организации (выданный менеджером).  
   - Введите **ФИО** (как отображаться в рейтингах).  
   - После успешной регистрации откроется главное меню продавца.

2. **Фиксация продажи**  
   - «✅ Фиксация продажи».  
   - Отображается список **незакреплённых** продаж вашей организации (из загруженной из 1С базы).  
   - Выберите продажу (кнопка с номером/наименованием) и подтвердите закрепление.  
   - Рейтинги пересчитываются автоматически.

3. **Профиль**  
   - «👤 Профиль» — рейтинг за всё время и за текущий месяц, сравнение с прошлым месяцем, прогресс челленджа и лига.

4. **Рейтинги**  
   - «🌍 Мировой рейтинг месяца» — таблица всех продавцов за текущий месяц.  
   - «🏢 Рейтинг в компании за месяц» — рейтинг внутри вашей организации за текущий месяц.

Если продавец забыл пароль — новый пароль задаёт менеджер в карточке организации («🔄 Сбросить пароль организации») и передаёт его продавцу.

---

## Интеграция с 1С

Бот обращается к HTTP-сервису 1С методом **POST** по адресу из `ONEC_URL`.

**Тело запроса (JSON):**
```json
{
  "startDate": "YYYY-MM-DD",
  "endDate": "YYYY-MM-DD",
  "operationType": "Передача между УОТ"
}
```

Значение `operationType` задаётся в `.env` переменной `ONEC_OPERATION_TYPE`. При необходимости 1С может принимать тот же параметр под ключом `ТипОперации` (бот может отправлять оба варианта).

**Ожидаемый ответ 1С (JSON):**
```json
{
  "ok": true,
  "count": 2,
  "rows": [
    {
      "Период": "2026-01-21",
      "ТипОперации": "Передача между УОТ",
      "Номенклатура": "...",
      "ОбъемТоваров": 3.0,
      "ОбъемЧастичнойРеализации": 0.0,
      "ПродавецИНН": "...",
      "ПродавецНаименование": "...",
      "ПокупательИНН": "...",
      "ПокупательНаименование": "..."
    }
  ]
}
```

При ошибке 1С может вернуть, например: `{"ok": false, "error": "текст ошибки"}`. Бот покажет этот текст менеджеру в сообщении об ошибке.

Если 1С опубликована с Basic Auth, укажите `ONEC_USERNAME` и `ONEC_PASSWORD` в `.env`.

---

## Модуль для 1С (расширение)

Ниже — описание и код модуля для 1С:ERP (расширение + HTTP-сервис в основной конфигурации), который предоставляет боту HTTP-точку для запроса оборотов за период и по типу операции.

### 1) Назначение и общая схема

**Цель:** дать Telegram-боту (работает через Polling) HTTP-точку в локальной сети, чтобы бот мог запрашивать данные из 1С:ERP за период и по типу операции (по умолчанию: «Передача между УОТ») и получать ответ в JSON.

**Архитектура (как сделано в рабочем варианте):**

- В **основной конфигурации** создаётся объект HTTP-сервис BotAPI, чтобы он гарантированно публиковался на веб-сервере и появлялся в списках публикации.
- В **модуле HTTP-сервиса** лежит тонкий прокси-вход `botapiМетод1()`, который делегирует обработку в код расширения.
- В **расширении** лежит вся логика: парсинг JSON, валидация дат, поиск значения ТипОперации, выполнение запроса к регистру, формирование ответа JSON.

Платформа понимает, что запрос идёт к HTTP-сервисам, по обязательному сегменту пути `/hs/`.

### 2) Требования

**2.1. Платформа и публикация**

1. Платформа 1С 8.3 (HTTP-сервисы и JSON доступны в 8.3.x; потоковые ЧтениеJSON/ЗаписьJSON — с 8.3.6).
2. Информационная база должна быть опубликована на веб-сервере (IIS/Apache), иначе URL вида `/hs/...` не будет доступен извне.

**2.2. Доступ по сети**

- Бот должен иметь сетевой доступ до веб-сервера, где опубликована база 1С: порт 80 (HTTP) или 443 (HTTPS).
- Желательно ограничить доступ firewall'ом: разрешить обращения к `/hs/BotAPI/*` только с IP, где крутится бот.

**2.3. Аутентификация**

- Рекомендуемый вариант: Basic Auth (логин/пароль пользователя 1С, который передаёт веб-сервер). При анонимной аутентификации на веб-сервере 1С может запрашивать Basic-авторизацию.
- Не хранить логин/пароль в репозитории. Держать в `.env`, секретах CI или локальном конфиге.

### 3) Что создаём в 1С

**3.1. Расширение (рекомендуемый подход)**

Чтобы не трогать типовую ERP на поддержке, логику держат в расширении.

1. Создайте расширение (Конфигуратор → Расширения конфигурации → Создать).
2. В расширении создайте **Общий модуль**: имя `BotAPI_Impl`, свойства: **Сервер = Да**, **Клиент = Нет**.
3. Вставьте туда код обработчика (см. раздел «Код» ниже).

На практике надёжный вариант — HTTP-сервис в **основной конфигурации** и вся логика в **расширении**.

**3.2. HTTP-сервис в основной конфигурации**

1. В конфигурации (не в расширении): **Общие → HTTP-сервисы → BotAPI**.
2. Внутри BotAPI создайте ресурс/шаблон URL: `chz_turnover`, метод **POST**.
3. В модуле `HTTPСервис.BotAPI.Модуль` разместите прокси-код входа (см. раздел «Код»).

### 4) Публикация базы на веб-сервере и итоговый URL

**4.1. Публикация**

База публикуется с именем публикации (виртуальный каталог/алиас). После публикации доступен URL:

```
http(s)://<WEB_HOST>/<PUBLISH_NAME>/hs/<HTTP_SERVICE>/<URL_TEMPLATE>
```

Где:

- `/hs` — признак HTTP-сервисов (обязательный сегмент);
- `<HTTP_SERVICE>` — имя объекта HTTP-сервиса (у нас `BotAPI`);
- `<URL_TEMPLATE>` — имя шаблона (у нас `chz_turnover`).

**Пример:** `http://192.168.0.217/1cerp_copy/hs/BotAPI/chz_turnover`

**4.2. Как понять «свой» адрес**

Нужны: IP/имя веб-сервера; имя публикации (виртуальный каталог, в примере `1cerp_copy`); в 1С — имя HTTP-сервиса `BotAPI` и шаблона `chz_turnover`.

### 5) Пользователь 1С и права

- Создайте отдельного пользователя (например `bot_api`) только для API.
- Минимально необходимо: право на вызов HTTP-сервиса; права чтения регистра `РегистрНакопления.ЧЗ_Оборачиваемость` и связанных справочников (Номенклатура, Продавец, Покупатель).
- Если включено RLS, пользователь может получать пустой результат — проверьте запрос, зайдя в 1С под `bot_api`.

### 6) Контракт API

**6.1. Запрос**

- Метод: **POST**
- Content-Type: `application/json`
- Тело (JSON): `startDate`, `endDate` — обязательны; форматы дат: YYYY-MM-DD или DD.MM.YYYY; `operationType` — опционален, по умолчанию «Передача между УОТ».

**6.2. Ответ 200 OK**

JSON с полями `ok`, `startDate`, `endDate`, `operationType`, `count`, `rows` (массив строк с полями Период, ТипОперации, Номенклатура, ОбъемТоваров, ОбъемЧастичнойРеализации, ПродавецИНН, ПродавецНаименование, ПокупательИНН, ПокупательНаименование).

**6.3. Ошибки**

- **400** — некорректные параметры (нет дат, неверный формат, период > 60 дней, не найден ТипОперации).
- **500** — внутренняя ошибка.

Формат ошибки: `{ "ok": false, "error": "..." }`. При 400 для «ТипОперации не найден» в ответ может добавляться массив `availableOperationTypes` с доступными представлениями.

### 7) Логика работы обработчика

В `BotAPI_Impl.HandleChzTurnoverPOST()`:

1. Читается тело запроса, парсится JSON в структуру.
2. Извлекаются `startDate`, `endDate`, опционально `operationType`.
3. Даты преобразуются, приводятся к НачалоДня/КонецДня; проверяется период (ограничение 60 дней).
4. Значение ТипОперации ищется по строковому представлению: нормализация строки (в т.ч. NBSP), поиск точного совпадения или подстроки среди ПРЕДСТАВЛЕНИЕ(ТипОперации) в регистре за период; при отсутствии — ответ 400.
5. Выполняется основной запрос по регистру ЧЗ_Оборачиваемость за период с фильтром по ТипОперации.
6. Формируется массив `rows`, возвращается HTTP 200 и JSON.

### 8) Код

**8.1. Основная конфигурация: HTTPСервис.BotAPI.Модуль**

- `botapiМетод1(Запрос)` — делегирует в расширение `BotAPI_Impl.HandleChzTurnoverPOST(Запрос)`.
- `СформироватьОшибкаJSON(...)` — формирование ответа с ошибкой 500 при исключении в прокси.

**8.2. Расширение: ОбщийМодуль.BotAPI_Impl (Сервер)**

- `HandleChzTurnoverPOST(Запрос)` — основной обработчик.
- `НайтиТипОперацииПоПредставлению(...)` — поиск значения по представлению (точное совпадение, при необходимости по подстроке при одном кандидате).
- `НормСтрока(...)` — нормализация строки (NBSP, табы, двойные пробелы, нижний регистр для сравнения).
- `ПреобразоватьВДатуИзAPI(...)` — разбор дат YYYY-MM-DD, DD.MM.YYYY и др.
- `СформироватьОшибка(...)`, `СформироватьОшибкаСВариантами(...)` — формирование JSON-ответов с ошибками.

Ниже приведён рабочий код модулей.

**Основная конфигурация: HTTPСервис.BotAPI.Модуль**

```bsl
Функция botapiМетод1(Запрос) Экспорт
	Попытка
		Возврат BotAPI_Impl.HandleChzTurnoverPOST(Запрос);
	Исключение
		Возврат СформироватьОшибкаJSON(500, "EXCP: " + ОписаниеОшибки());
	КонецПопытки;
КонецФункции

Функция СформироватьОшибкаJSON(КодСостояния, Сообщение) Экспорт
	Ответ = Новый HTTPСервисОтвет(КодСостояния);
	Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
	Тело = Новый Структура;
	Тело.Вставить("ok", Ложь);
	Тело.Вставить("error", Сообщение);
	Запись = Новый ЗаписьJSON;
	Запись.УстановитьСтроку();
	ЗаписатьJSON(Запись, Тело);
	ТекстJSON = Запись.Закрыть();
	Ответ.УстановитьТелоИзСтроки(ТекстJSON, КодировкаТекста.UTF8);
	Возврат Ответ;
КонецФункции
```

**Расширение: ОбщийМодуль.BotAPI_Impl (фрагмент — HandleChzTurnoverPOST и вспомогательные функции)**

Полный код расширения включает: чтение тела запроса и парсинг JSON; проверку полей `startDate`, `endDate` и опционального `operationType` (по умолчанию «Передача между УОТ»); преобразование дат и проверку периода (не более 60 дней); вызов `НайтиТипОперацииПоПредставлению` для получения значения ТипОперации по строке (при отсутствии — ответ 400 с возможным списком `availableOperationTypes`); выполнение запроса к `РегистрНакопления.ЧЗ_Оборачиваемость` за период с фильтром по ТипОперации; сбор массива строк с полями Период, ТипОперации, Номенклатура, ОбъемТоваров, ОбъемЧастичнойРеализации, ПродавецИНН, ПродавецНаименование, ПокупательИНН, ПокупательНаименование; формирование ответа 200 и JSON.

Вспомогательные функции расширения:

- **НайтиТипОперацииПоПредставлению** — запрос РАЗЛИЧНЫЕ ТипОперации и ПРЕДСТАВЛЕНИЕ за период; нормализация через `НормСтрока`; сначала точное совпадение, при отсутствии — поиск по подстроке, если кандидат один; возврат значения или Неопределено.
- **ДобавитьУникально** — добавление строки в массив без дубликатов.
- **НормСтрока** — замена NBSP(160), табов, переводов строк на пробел; СокрЛП; НРег; сжатие двойных пробелов.
- **СформироватьОшибка** / **СформироватьОшибкаСВариантами** — формирование HTTP-ответа с JSON `{ "ok": false, "error": "..." }`, при вариантах — дополнительно `availableOperationTypes`.
- **ПреобразоватьВДатуИзAPI** — разбор YYYY-MM-DD, DD.MM.YYYY, YYYYMMDD и др.; возврат Даты или Неопределено.
- **ЭтоЦифры** — проверка, что строка состоит только из цифр.

**Расширение: ОбщийМодуль.BotAPI_Impl (Сервер = Да)**

Полный код модуля расширения (без раскрывающегося блока — сразу виден на GitHub):

```bsl
// ОбщийМодуль.BotAPI_Impl (Сервер = Да)

Функция HandleChzTurnoverPOST(Запрос) Экспорт

	// 1) читаем тело запроса
	Попытка
		Тело = Запрос.ПолучитьТелоКакСтроку();
	Исключение
		Возврат СформироватьОшибка(400, "Не удалось прочитать тело запроса");
	КонецПопытки;

	Если СтрДлина(СокрЛП(Тело)) = 0 Тогда
		Возврат СформироватьОшибка(400, "Пустое тело запроса");
	КонецЕсли;

	// 2) JSON -> структура
	Попытка
		ЧтениеJSON = Новый ЧтениеJSON;
		ЧтениеJSON.УстановитьСтроку(Тело);
		Парам = ПрочитатьJSON(ЧтениеJSON);
		ЧтениеJSON.Закрыть();
	Исключение
		Возврат СформироватьОшибка(400, "Некорректный JSON");
	КонецПопытки;

	Если ТипЗнч(Парам) <> Тип("Структура") Тогда
		Возврат СформироватьОшибка(400, "Ожидался JSON-объект со свойствами startDate, endDate, operationType");
	КонецЕсли;

	// ВАЖНО: переменные-«приёмники» должны быть объявлены до передачи во 2-й параметр Свойство()
	СтрНачало = Неопределено;
	СтрКонец  = Неопределено;

	Если НЕ Парам.Свойство("startDate", СтрНачало) ИЛИ НЕ Парам.Свойство("endDate", СтрКонец) Тогда
		Возврат СформироватьОшибка(400, "Нужны поля startDate и endDate");
	КонецЕсли;

	// operationType (внешний текст). По умолчанию — как требовалось.
	СтрТипОперации = "Передача между УОТ";
	ОперацияИзJSON = Неопределено;

	Если Парам.Свойство("operationType", ОперацияИзJSON) Тогда
		Если ТипЗнч(ОперацияИзJSON) = Тип("Строка") И СтрДлина(СокрЛП(ОперацияИзJSON)) > 0 Тогда
			СтрТипОперации = ОперацияИзJSON;
		КонецЕсли;
	КонецЕсли;

	НачалоПериода = ПреобразоватьВДатуИзAPI(СтрНачало);
	КонецПериода  = ПреобразоватьВДатуИзAPI(СтрКонец);

	Если НачалоПериода = Неопределено ИЛИ КонецПериода = Неопределено Тогда
		Возврат СформироватьОшибка(400, "Неверный формат дат. Поддерживается YYYY-MM-DD или DD.MM.YYYY");
	КонецЕсли;

	НачалоПериода = НачалоДня(НачалоПериода);
	КонецПериода  = КонецДня(КонецПериода);

	Если НачалоПериода > КонецПериода Тогда
		Возврат СформироватьОшибка(400, "startDate больше endDate");
	КонецЕсли;

	// Защита от огромных периодов (60 дней)
	Если (КонецПериода - НачалоПериода) > 60 * 24 * 60 * 60 Тогда
		Возврат СформироватьОшибка(400, "Слишком большой период (ограничение 60 дней)");
	КонецЕсли;

	// 3) Находим реальное значение ТипОперации по строковому представлению
	ДоступныеТипы = Новый Массив;

	Попытка
		ТипОперацииЗначение = НайтиТипОперацииПоПредставлению(НачалоПериода, КонецПериода, СтрТипОперации, ДоступныеТипы);
	Исключение
		Возврат СформироватьОшибка(500, "Ошибка поиска ТипОперации по представлению: " + ОписаниеОшибки());
	КонецПопытки;

	Если ТипОперацииЗначение = Неопределено Тогда
		Возврат СформироватьОшибкаСВариантами(400,
			"ТипОперации не найден по представлению: " + СтрТипОперации,
			ДоступныеТипы);
	КонецЕсли;

	// 4) Основной запрос
	ЗапросДанных = Новый Запрос;
	ЗапросДанных.Текст =
	"ВЫБРАТЬ
	|	ЧЗ_Оборачиваемость.Период КАК Период,
	|	ПРЕДСТАВЛЕНИЕ(ЧЗ_Оборачиваемость.ТипОперации) КАК ТипОперации,
	|	ПРЕДСТАВЛЕНИЕ(ЧЗ_Оборачиваемость.Номенклатура) КАК Номенклатура,
	|	ЧЗ_Оборачиваемость.ОбъемТоваров КАК ОбъемТоваров,
	|	ЧЗ_Оборачиваемость.ОбъемЧастичнойРеализации КАК ОбъемЧастичнойРеализации,
	|	ЧЗ_Оборачиваемость.Продавец.ИНН КАК ПродавецИНН,
	|	ЧЗ_Оборачиваемость.Продавец.Наименование КАК ПродавецНаименование,
	|	ЧЗ_Оборачиваемость.Покупатель.ИНН КАК ПокупательИНН,
	|	ЧЗ_Оборачиваемость.Покупатель.Наименование КАК ПокупательНаименование
	|ИЗ
	|	РегистрНакопления.ЧЗ_Оборачиваемость КАК ЧЗ_Оборачиваемость
	|ГДЕ
	|	ЧЗ_Оборачиваемость.Период МЕЖДУ &НачалоПериода И &КонецПериода
	|	И ЧЗ_Оборачиваемость.ТипОперации = &ТипОперации";

	ЗапросДанных.УстановитьПараметр("НачалоПериода", НачалоПериода);
	ЗапросДанных.УстановитьПараметр("КонецПериода",  КонецПериода);
	ЗапросДанных.УстановитьПараметр("ТипОперации",   ТипОперацииЗначение);

	Попытка
		Рез = ЗапросДанных.Выполнить();
		Выборка = Рез.Выбрать();
	Исключение
		Возврат СформироватьОшибка(500, "Ошибка выполнения запроса 1С: " + ОписаниеОшибки());
	КонецПопытки;

	Строки = Новый Массив;

	Пока Выборка.Следующий() Цикл
		Стр = Новый Структура;

		Стр.Вставить("Период", Формат(Выборка.Период, "ДФ=yyyy-MM-dd"));
		Стр.Вставить("ТипОперации", Выборка.ТипОперации);
		Стр.Вставить("Номенклатура", Выборка.Номенклатура);

		Стр.Вставить("ОбъемТоваров", Выборка.ОбъемТоваров);
		Стр.Вставить("ОбъемЧастичнойРеализации", Выборка.ОбъемЧастичнойРеализации);

		Стр.Вставить("ПродавецИНН", Выборка.ПродавецИНН);
		Стр.Вставить("ПродавецНаименование", Выборка.ПродавецНаименование);
		Стр.Вставить("ПокупательИНН", Выборка.ПокупательИНН);
		Стр.Вставить("ПокупательНаименование", Выборка.ПокупательНаименование);

		Строки.Добавить(Стр);
	КонецЦикла;

	// 5) Ответ JSON
	ОтветСтруктура = Новый Структура;
	ОтветСтруктура.Вставить("ok", Истина);
	ОтветСтруктура.Вставить("startDate", Формат(НачалоПериода, "ДФ=yyyy-MM-dd"));
	ОтветСтруктура.Вставить("endDate",   Формат(КонецПериода,  "ДФ=yyyy-MM-dd"));
	ОтветСтруктура.Вставить("operationType", СтрТипОперации);
	ОтветСтруктура.Вставить("count", Строки.Количество());
	ОтветСтруктура.Вставить("rows", Строки);

	ЗаписьJSON = Новый ЗаписьJSON;
	ЗаписьJSON.УстановитьСтроку();
	ЗаписатьJSON(ЗаписьJSON, ОтветСтруктура);
	ТелоОтвета = ЗаписьJSON.Закрыть();

	Ответ = Новый HTTPСервисОтвет(200);
	Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
	Ответ.УстановитьТелоИзСтроки(ТелоОтвета, КодировкаТекста.UTF8, ИспользованиеByteOrderMark.НеИспользовать);
	Возврат Ответ;

КонецФункции


Функция НайтиТипОперацииПоПредставлению(НачалоПериода, КонецПериода, ПредставлениеТипОперации, ДоступныеТипы) Экспорт

	Если ДоступныеТипы = Неопределено Тогда
		ДоступныеТипы = Новый Массив;
	КонецЕсли;

	Если ТипЗнч(ПредставлениеТипОперации) <> Тип("Строка") Тогда
		Возврат Неопределено;
	КонецЕсли;

	Искомое = НормСтрока(ПредставлениеТипОперации);
	Если СтрДлина(Искомое) = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;

	ЗапросТип = Новый Запрос;
	ЗапросТип.Текст =
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ЧЗ.ТипОперации КАК ТипОперации,
	|	ПРЕДСТАВЛЕНИЕ(ЧЗ.ТипОперации) КАК ТипОперацииПредставление
	|ИЗ
	|	РегистрНакопления.ЧЗ_Оборачиваемость КАК ЧЗ
	|ГДЕ
	|	ЧЗ.Период МЕЖДУ &НачалоПериода И &КонецПериода";

	ЗапросТип.УстановитьПараметр("НачалоПериода", НачалоПериода);
	ЗапросТип.УстановитьПараметр("КонецПериода",  КонецПериода);

	Рез = ЗапросТип.Выполнить();

	// 1) точное совпадение
	Выб = Рез.Выбрать();
	Пока Выб.Следующий() Цикл
		ИсхПред = "" + Выб.ТипОперацииПредставление;
		ДобавитьУникально(ДоступныеТипы, ИсхПред);

		ТекПред = НормСтрока(ИсхПред);
		Если ТекПред = Искомое Тогда
			Возврат Выб.ТипОперации;
		КонецЕсли;
	КонецЦикла;

	// 2) если точного нет — поиск по подстроке (но только если найден ровно 1 кандидат)
	Выб = Рез.Выбрать();
	Кандидат = Неопределено;
	КолКандидатов = 0;

	Пока Выб.Следующий() Цикл
		ТекПред = НормСтрока("" + Выб.ТипОперацииПредставление);
		Если СтрНайти(ТекПред, Искомое) > 0 Тогда
			КолКандидатов = КолКандидатов + 1;
			Кандидат = Выб.ТипОперации;
		КонецЕсли;
	КонецЦикла;

	Если КолКандидатов = 1 Тогда
		Возврат Кандидат;
	КонецЕсли;

	Возврат Неопределено;

КонецФункции


Процедура ДобавитьУникально(МассивСтрок, ЗначениеСтроки) Экспорт

	Если МассивСтрок = Неопределено Тогда Возврат; КонецЕсли;
	Если ЗначениеСтроки = Неопределено Тогда Возврат; КонецЕсли;

	С = "" + ЗначениеСтроки;
	Если СтрДлина(СокрЛП(С)) = 0 Тогда Возврат; КонецЕсли;

	Для каждого Э Из МассивСтрок Цикл
		Если Э = С Тогда
			Возврат;
		КонецЕсли;
	КонецЦикла;

	МассивСтрок.Добавить(С);

КонецПроцедуры


Функция НормСтрока(С) Экспорт

	Если С = Неопределено Тогда Возврат ""; КонецЕсли;
	Если ТипЗнч(С) <> Тип("Строка") Тогда
		С = Строка(С);
	КонецЕсли;

	Р = С;

	// NBSP(160), табы, переводы строк -> пробел
	Р = СтрЗаменить(Р, Символ(160), " ");
	Р = СтрЗаменить(Р, Символ(9),   " ");
	Р = СтрЗаменить(Р, Символ(10),  " ");
	Р = СтрЗаменить(Р, Символ(13),  " ");

	Р = СокрЛП(Р);
	Р = НРег(Р);

	Пока СтрНайти(Р, "  ") > 0 Цикл
		Р = СтрЗаменить(Р, "  ", " ");
	КонецЦикла;

	Возврат Р;

КонецФункции


Функция СформироватьОшибкаСВариантами(Код, Сообщение, ДоступныеТипы) Экспорт

	Ош = Новый Структура;
	Ош.Вставить("ok", Ложь);
	Ош.Вставить("error", Сообщение);

	Если ДоступныеТипы <> Неопределено Тогда
		Ош.Вставить("availableOperationTypes", ДоступныеТипы);
	КонецЕсли;

	ЗаписьJSON = Новый ЗаписьJSON;
	ЗаписьJSON.УстановитьСтроку();
	ЗаписатьJSON(ЗаписьJSON, Ош);
	ТелоОтвета = ЗаписьJSON.Закрыть();

	Ответ = Новый HTTPСервисОтвет(Код);
	Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
	Ответ.УстановитьТелоИзСтроки(ТелоОтвета, КодировкаТекста.UTF8, ИспользованиеByteOrderMark.НеИспользовать);
	Возврат Ответ;

КонецФункции


Функция СформироватьОшибка(Код, Сообщение) Экспорт

	Ош = Новый Структура;
	Ош.Вставить("ok", Ложь);
	Ош.Вставить("error", Сообщение);

	ЗаписьJSON = Новый ЗаписьJSON;
	ЗаписьJSON.УстановитьСтроку();
	ЗаписатьJSON(ЗаписьJSON, Ош);
	ТелоОтвета = ЗаписьJSON.Закрыть();

	Ответ = Новый HTTPСервисОтвет(Код);
	Ответ.Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
	Ответ.УстановитьТелоИзСтроки(ТелоОтвета, КодировкаТекста.UTF8, ИспользованиеByteOrderMark.НеИспользовать);
	Возврат Ответ;

КонецФункции


Функция ПреобразоватьВДатуИзAPI(СтрДата) Экспорт

	Если СтрДата = Неопределено Тогда Возврат Неопределено; КонецЕсли;

	С = СокрЛП(СтрДата);
	Если СтрДлина(С) = 0 Тогда Возврат Неопределено; КонецЕсли;

	// YYYY-MM-DD
	Если СтрДлина(С) = 10 И Сред(С, 5, 1) = "-" И Сред(С, 8, 1) = "-" Тогда
		Канон = СтрЗаменить(С, "-", "");
		Попытка Возврат Дата(Канон); Исключение Возврат Неопределено; КонецПопытки;
	КонецЕсли;

	// DD.MM.YYYY
	Если СтрДлина(С) = 10 И Сред(С, 3, 1) = "." И Сред(С, 6, 1) = "." Тогда
		День  = Сред(С, 1, 2);
		Месяц = Сред(С, 4, 2);
		Год   = Сред(С, 7, 4);
		Канон = Год + Месяц + День;
		Попытка Возврат Дата(Канон); Исключение Возврат Неопределено; КонецПопытки;
	КонецЕсли;

	// YYYYMMDD или YYYYMMDDHHMMSS
	Если (СтрДлина(С) = 8 ИЛИ СтрДлина(С) = 14) И ЭтоЦифры(С) Тогда
		Попытка Возврат Дата(С); Исключение Возврат Неопределено; КонецПопытки;
	КонецЕсли;

	Попытка
		Возврат Дата(С);
	Исключение
		Возврат Неопределено;
	КонецПопытки;

КонецФункции


Функция ЭтоЦифры(С) Экспорт

	Для i = 1 По СтрДлина(С) Цикл
		Сим = Сред(С, i, 1);
		Если Сим < "0" ИЛИ Сим > "9" Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;

	Возврат Истина;

КонецФункции
```

### 9) Проверка работоспособности (чек-лист)

- **Сеть:** проверка TCP-порта 80/443 (пинг может быть отключён).
- **URL:** при 404 проверить имя публикации, имя HTTP-сервиса (BotAPI), имя шаблона (chz_turnover).
- **Авторизация:** при 401 — логин/пароль или настройки аутентификации веб-сервера и 1С.
- **Права:** при 200 и `count=0` при наличии данных в консоли — войти в 1С под `bot_api`, проверить RLS и права на регистр.

### 10) Типовые проблемы и диагностика

1. **401 Unauthorized** — обычно Basic Auth, пользователь или настройка IIS/Apache.
2. **500 Internal Server Error** — смотреть журнал регистрации 1С и текст `ОписаниеОшибки()`; в коде ошибки упаковываются в JSON.
3. **ТипОперации «не найден»** — отличия в пробелах/NBSP; нет прав на записи за период (RLS); в периоде нет строк с такой операцией.
4. **Таймауты** — нагрузка веб-сервера/рабочего процесса 1С; при необходимости логировать время старта и окончания в обработчике.

---

## Хранение данных

- **База данных** — SQLite, путь задаётся в `DB_PATH` (по умолчанию `./data/bot.sqlite3`). Папка `data/` создаётся при первом запуске.
- **Логи** — файл по пути `LOG_PATH` (по умолчанию `./logs/bot.log`), ротация по размеру (1 МБ, до 3 резервных копий).
- Таблицы, в том числе: организации, пользователи (продавцы), обороты из 1С (`chz_turnover`), закрепления продаж, рейтинги за всё время и по месяцам, челленджи, уведомления, аудит.

---

## Расписание задач

Все задачи выполняются по времени **МСК** (часовой пояс задан в коде бота).

| Задача              | Расписание        | Описание |
|---------------------|-------------------|----------|
| Синхронизация с 1С  | Воскресенье 04:00 | Загрузка оборотов за последние 30 дней (если задан `ONEC_URL`). |
| Месячный снапшот    | 1-е число 00:10   | Сохранение снимка рейтингов за прошедший месяц. |
| Челленджи           | 1-е и 15-е число 00:05 | Создание/обновление двухнедельных челленджей для продавцов. |
| Напоминания         | Ежедневно 10:00   | Напоминания продавцам (если не «тихие часы»): не заходил 3+ дня, потеря места, близкий конкурент. |

---

## Тестовые данные

Чтобы проверить бота без 1С, можно загрузить тестовые обороты в SQLite:

```bash
python seed_test_turnover.py
```

Скрипт создаёт таблицы (если их ещё нет) и добавляет несколько записей в `chz_turnover`. Для отображения и фиксации продаж продавцом нужно создать организацию с ИНН **4611012110** (как в тестовых данных) и зарегистрировать продавца с этим ИНН и паролем организации.

---

## Устранение неполадок

- **«ONEC_URL не задан»** — в `.env` не указан или не подхватился `ONEC_URL`. Убедитесь, что `.env` лежит в каталоге проекта (рядом с `bot.py`) и переменная задана без лишних пробелов. После изменения перезапустите бота.

- **«BOT_TOKEN is required»** — в `.env` нет `BOT_TOKEN` или он пустой. Получите токен у [@BotFather](https://t.me/BotFather) и запишите в `.env`.

- **«Conflict: terminated by other getUpdates request»** — с тем же токеном уже запущен другой экземпляр бота (другой терминал, сервер или приложение). Остановите все остальные процессы с этим ботом или подождите — текущий экземпляр будет повторять запросы и подхватит обновления, когда другое подключение закроется.

- **Продавец не может войти** — проверьте ИНН и пароль. Если пароль забыт — менеджер в карточке организации нажимает «🔄 Сбросить пароль организации» и передаёт новый пароль продавцу.

- **Ошибка 1С при обновлении базы** — текст ошибки бот выводит менеджеру в чат. Типичные причины: 1С недоступна по сети, неверный URL/логин/пароль, ошибка в коде 1С (например, поиск типа операции по представлению). Проверьте доступность `ONEC_URL`, учётные данные и логи 1С.

- **Пароль в .env со спецсимволами** — заключите значение в двойные кавычки, например: `ONEC_PASSWORD="mWS4nD@x3zwe~|}"`.

---

## Лицензия и репозиторий

Проект: [Distro-Rating-Bot](https://github.com/deadsmokeprod/Distro-Rating-Bot).
